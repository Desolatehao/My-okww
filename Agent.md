# Agent.md — 开轴流程与工作约定

这份文档写给**下一次开新轴时的自己**（以及 AI 协作者）。

弗卜维轴（卜灵 / 弗洛洛 / 维里奈）从 1.0.1 迭代到 1.2.0，一共出了 9 个版本才稳定。
下面这套流程就是从那 9 个版本里总结出来的——照着走能少踩一遍同样的坑。
通用的框架机制（切人优先级、CD 判定、前后摇、空中攻击等）单独整理在
[`开发知识/OKWW机制笔记.md`](开发知识/OKWW机制笔记.md)。

---

## 0. 仓库结构约定

```text
<轴名>v<版本>/              当前交付物：脚本 + 文档
<轴名>v<版本>.zip           同名成品包，内容与文件夹一致
参考资料/<轴名>涉及资料/      这条轴读过的原始资料 + README.md
ok-wuthering-waves-custom/  可运行工作副本（历史版本归档放这里）
开发知识/                   跨轴通用的机制笔记
```

- 一条轴一个顶层文件夹，版本号写进名字。
- **当前版本放根目录**，旧版本归档进 `ok-wuthering-waves-custom/axis_<...>/`，不要散在根目录。
- 参考资料按轴分文件夹，当前的在 `参考资料/弗卜维涉及资料/`。新轴就并排建 `<轴名>涉及资料/`。
- 上游 OKWW 代码永远不进这个仓库（`.gitignore` 已忽略 `ok-wuthering-waves/`）。

---

## 1. 开轴总流程

### 阶段 A：先收集输入，别急着写代码

1. 确认目标队伍和**动作顺序**。最好的输入是作者本人的操作轴文本或连招图（`.wwcombo.json` 这种）。
2. 收集参考包：别人写过同角色或同机制的实现，全部放进 `参考资料/<轴名>涉及资料/`，
   并在该文件夹的 `README.md` 里逐条写清楚**每份资料具体提供什么**、可靠性如何。
   弗卜维轴就是靠三份参考包分别解决卜灵、弗洛洛、维里奈的时序。
3. 建 `MANUAL_TIMELINE_24FPS.md`。手操录像按帧记录，时间码格式 `00:00:00:00`，末两位是帧号。

### 阶段 B：抠时序（决定这条轴成不成）

**分清三种证据，绝对不要混用：**

| 证据 | 可靠性 | 例子 |
| --- | --- | --- |
| 逐帧派生点 | 最可靠 | 弗洛洛 A1/A2/A3 = 40/36/32 帧 |
| 参考实现的等待值 | 次可靠 | 卜灵 `2.5s` 长按、E 后 `0.2s`——是**实现值**不是帧值 |
| 手操录像目测 | 最不确定 | 无键鼠叠加层，±1 帧起步 |

**分清两种时间：**

- **输入间隔**：同一种输入之间能发多快。
- **可接动作窗口**：这个动作开始后多久才允许发下一个**不同**动作。

轴里真正卡住的是后者。每个角色、每种动作都要单独定，**不能全队一个常数**。

### 阶段 C：先写阶段表，再写代码

- 用 `phase` 编号：启动轴 `0..N`，循环轴从一个入口 phase 开始，末尾指回入口。
- 每个 phase 写清楚三件事：**谁出手 / 做什么动作 / 下一步切给谁**。
- 动作记号统一：`a`/`A` 普攻、`z` 重击、`e` 共鸣技能、`q` 声骸、`r` 共鸣解放、`闪`、`跳`。
- 表写不出来就别写代码——`TIMING.md` 里那张阶段表就是代码的唯一依据。

### 阶段 D：实现

- **队伍 gate**：只在检测到本轴的固定队伍时接管，其他队伍保持各自原本的逻辑。
- 参数全部提到类顶部 `AXIS_*` 常量，**中文注释**，标明用途、阶段和单位。
- 每个 phase 用**持久化 step 游标**：动作失败就保留在当前 step 重试，
  不要跳过或提前进入下一个 phase（早期版本就是因为"重试到超时然后跳段"出过问题）。
- 共享状态放 task 级字典（弗卜维轴用 `_dpv_axis_state`），在 `on_combat_end` 里清理干净。
- 样板：直接照 [`弗卜维施法顺序v1.2.0/`](弗卜维施法顺序v1.2.0) 里的三个脚本抄结构。

### 阶段 E：实机迭代（最花时间的一步）

每次实机跑完，三件事一起做：

1. 改 `AXIS_*` 常量——**只改要改的**，不要顺手动别的 phase。
2. 写/更新 `PROGRESS_<版本>.md`：这次改了什么、实机观察到什么、对应录像的哪个时间码。
3. 出包：`<轴名>_<版本>/` 文件夹 + 同名 zip（zip 排除 `__pycache__`）。

两条经验：

- **每次实机验证通过就 +1 版本**。弗卜维轴 1.0.6 → 1.0.7 → 1.0.8 → 1.1.0 → 1.1.1 都是这么来的。
- **把"录像值 vs 实机值"的差异写下来**。轴的手感和录像经常对不上：弗洛洛 Z 录像是 `1.33s`、
  旧参考包却是 `2.32s`；维里奈一个 `1.60s` 的值实战偶发失败，最后定 `1.75s`。
  实机值才是最终值，但录像值要留着，方便下次复测。

### 阶段 F：封版与整理

- 当前版本提到根目录，旧版本归档。
- 更新本文档、`开发知识/`、以及 `参考资料/<轴名>涉及资料/README.md`。
- 早期版本的压缩包可以删（内容和文件夹重复），文件夹留着当历史。

---

## 2. 踩过的坑（来自弗卜维轴的排错记录）

- **别把状态提前改好**。槽位直切必须等 `task.in_team()` 确认当前 `index` 之后再更新
  `is_current_char` / `has_intro`；提前改会让框架和游戏不同步，比多等一会儿更糟。
- **切人不要只依赖 `switch_next_char()` 的目标选择**。解放/声骸动画期间目标 HUD 会消失，
  框架会报 `target lost` 并卡住。用槽位直切 + `in_team()` 确认。
- **治疗者满协奏切人锁**。默认有 16 秒锁，会把卜灵↔维里奈之间的短切拦掉；
  本轴在 `healer_full_con_switch_locked()` 里返回 False 关掉它。
- **入场动画期间不要注入计划外普攻**。用 `wait_intro(click=False)`，让 phase 表独占所有攻击。
- **键位映射对不上会"看起来卡住"**。本地 `Q=声骸`、`R=解放`；反过来会让某个 phase 停在等技能。
- **动作不可用时的等待上限必须设**，否则会长时间发呆；但也不能短到把该放的技能跳过。
- **重击在空中会被打断**。重击前先 `if self.flying(): self.wait_down()`，被打断要重试。

---

## 3. 交付物清单

每个版本文件夹应包含：

| 文件 | 作用 |
| --- | --- |
| `Douling.py` / `Phrolova.py` / `<角色>.py` | 各角色的实装脚本 |
| `team.json` | 队伍导入清单（名称、版本、成员） |
| `Agent.md` | 这条轴自己的部署方法、状态机说明、运行时检查清单 |
| `TIMING.md` | 时序总表 + 证据来源（**改参数必须同步改它**） |
| `MANUAL_TIMELINE_24FPS.md` | 目标录像的手工时间线 |
| `PROGRESS_<版本>.md` | 本版本的改动与实测结果 |
| `角色资料/` | 涉及角色的技能机制文案 |
| `开发资料/` | 开发过程汇总、提交记录 |

---

## 4. 通用机制

切人、CD、平A 前后摇、跳A、切人后自动平A 等内容见
[`开发知识/OKWW机制笔记.md`](开发知识/OKWW机制笔记.md)。开新轴之前先过一遍。

---

## 5. 提交与文档约定

- 一个版本一次提交，提交信息里写清楚改了哪个 phase（例如 `Fix DPV phase 14 three-attack inputs`）。
- 不要提交上游 OKWW 代码、`__pycache__`、`.DS_Store`。
- 参数注释用中文，标明阶段、单位、含义。
- **只改注释时**不要动可执行 token、参数值、动作顺序和状态机行为。
